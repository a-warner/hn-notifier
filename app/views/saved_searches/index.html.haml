%h1 Your saved searches

%table.table
  %thead
    %tr
      %th Query
      %th


  %tbody
    - saved_searches.each do |saved_search|
      %tr
        %td= link_to saved_search.query, search_path(q: saved_search.query)
        %td= link_to 'Destroy', saved_search, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New User search', new_saved_search_path
